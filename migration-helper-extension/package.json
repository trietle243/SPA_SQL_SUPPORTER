{
  "name": "migration-helper-extension",
  "displayName": "Migration Helper",
  "publisher": "Triet.LeTM",
  "description": "Validates SQL migration files locally for SPA",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "migration.validate",
        "title": "Validate Migration File"
      }
    ]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/node": "^18.x"
  }
}